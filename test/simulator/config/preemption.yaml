name: "Preemption"
duration: "2m"
tick_interval: "1s"
closed_loop: true
topology:
  clusters:
    - id: "small-cluster"
      nodes: 1
      gpus_per_node: 8
apps:
  - id: "low-pri"
    priority: 10
    gpus_per_replica: 2
    min_replicas: 0
    sla_ttft: "500ms"
    cold_start: "2s"
    throughput_per_replica: 10.0
    profile: "test/fixtures/profiles/llama2-7b.json"
  - id: "high-pri"
    priority: 0
    gpus_per_replica: 4
    min_replicas: 0
    sla_ttft: "200ms"
    cold_start: "2s"
    throughput_per_replica: 5.0
    profile: "test/fixtures/profiles/llama2-70b.json"
workload: "test/simulator/traces/preemption.csv"
